location @test {
    rewrite ^(.*)$ /test$1 break;
    error_page 404 = @dynamiccycletour;
}

location @dynamiccycletour {
        #proxy_pass http://content.presspage.com$uri;
        rewrite ^/test/(.*)$ http://content.presspage.com/$1 break;
}
client_max_body_size 100m;
